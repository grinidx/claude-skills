# Floor is 2.32.4, not the newest release: that is past the .netrc credential
# leak (CVE-2024-47081) and the verify=False fix (CVE-2024-35195), while still
# resolving on Python 3.9, whose ceiling is 2.32.5 (requests 2.33+ needs 3.10).
# Deliberately not pinned tighter -- 3.9 then installs 2.32.x and 3.13 installs
# the current release, so CI exercises both.
requests>=2.32.5
